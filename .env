# Database connection URL
# Format: mysql://USER:PASSWORD@HOST:PORT/DATABASE_NAME
DATABASE_URL="mysql://root:123456@localhost:3306/repair_system_db"

# JWT Secret Key — เปลี่ยนเป็น string แบบสุ่มก่อน deploy production
JWT_SECRET="change-this-to-a-random-secret-in-production"

# Server Port
PORT=3000

# SMTP Email (Nodemailer)
# สำหรับ Gmail: SMTP_HOST=smtp.gmail.com, SMTP_PORT=587, SMTP_SECURE=false
# สำหรับ Test (ไม่ส่งจริง): ใช้ Ethereal จาก https://ethereal.email
SMTP_HOST=smtp.ethereal.email
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=deontae.koch@ethereal.email
SMTP_PASS=QabncapkVd5ayNThQu
SMTP_FROM="ระบบแจ้งซ่อม" <no-reply@repair.local>
